# Optional: override images pulled from Docker Hub
# Format: <dockerhub-username-or-org>/<repo>:<tag>
BACKEND_IMAGE=dovidtroppe/soldier-housing-backend:latest
FRONTEND_IMAGE=dovidtroppe/soldier-housing-frontend:prod

# Example: pin to v1 tags
# BACKEND_IMAGE=dovidtroppe/soldier-housing-backend:v1
# FRONTEND_IMAGE=dovidtroppe/soldier-housing-frontend:v1

# Optional runtime settings (compose defaults are fine for local)
# SECRET_KEY=devsecret
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=changeme
# VITE_API_URL=http://localhost:8000

# Email (Resend) - required for all outbound emails
# - Create a Resend account and verify your domain/sender
# - Add these in Railway variables (and locally if you want to test emails)
#
# RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXX
# RESEND_FROM="Soldier Housing <no-reply@yourdomain.com>"
# RESEND_REPLY_TO=support@yourdomain.com
