# Optional: override images pulled from Docker Hub
# Format: <dockerhub-username-or-org>/<repo>:<tag>
BACKEND_IMAGE=dovidtroppe/soldier-housing-backend:latest
FRONTEND_IMAGE=dovidtroppe/soldier-housing-frontend:prod

# Example: pin to v1 tags
# BACKEND_IMAGE=dovidtroppe/soldier-housing-backend:v1
# FRONTEND_IMAGE=dovidtroppe/soldier-housing-frontend:v1

# Optional runtime settings (compose defaults are fine for local)
# SECRET_KEY=devsecret
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=changeme
# VITE_API_URL=http://localhost:8000

# SMTP (for password reset emails)
# If SMTP_HOST is not set, the backend prints the reset code to logs.
#
# Gmail (recommended: use an App Password, not your normal password)
# 1) Create a dedicated account like: noreply.yourapp@gmail.com
# 2) Enable 2‑Step Verification on that account
# 3) Create an App Password (Google Account -> Security -> App passwords)
# 4) Put the values below into Railway (or a local .env)
#
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_TLS=true
# SMTP_SSL=false
# SMTP_USER=noreply.yourapp@gmail.com
# SMTP_PASSWORD=YOUR_16_CHAR_APP_PASSWORD
# SMTP_FROM="Soldier Housing <noreply.yourapp@gmail.com>"
